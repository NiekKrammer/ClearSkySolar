{% extends 'base.html.twig' %}

{% block body %}
    {% for message in app.flashes('order_success') %}
        <div class="alert alert-success mx-auto my-5 p-4 text-center" style="width: fit-content;">
            {{ message }}
        </div>
    {% endfor %}
    <div class="herosection">
        <div class="row d-flex align-items-center justify-content-center text-center">
            <div class="col-xl-6 my-5">
                <h2 data-aos="fade-right" data-aos-duration="1200">Ontdek de Kracht van Zonne-energie
                    en begin je Duurzame Toekomst met <span>ClearSky Solar!</span>
                </h2>
            </div>
            <div class="col-xl-6 d-flex align-items-center justify-content-center" data-aos="fade-up-left"
                 data-aos-duration="1200">
                <img src="assets/images/page/solar_panel.jpg" class="hero_img">
            </div>
        </div>
    </div>

    <div class="container products">
        <div class="row">
            <h3 class="text-center">Onze producten</h3>
            <p class="text-center px-5">Een afspraak kan op de bestelpagina
                worden gemaakt.</p>

            {% for product in products %}
                <div class="d-flex my-3 product_card col-12 mx-auto" data-aos="fade-up" data-aos-duration="1000">
                    <img src="{{ asset('product_imgs/' ~ product.image_file) }}" class="product_img"
                         alt="{{ product.name }}">
                    <div class="d-flex flex-column mt-3">
                        <h3>{{ product.name }}</h3>
                        <p>{{ product.description }}</p>
                        <div class="mt-auto card_div">
                            <p class="fw-medium price">€ {{ product.price }}</p>
                            <a href="{{ path('product_view', {'name': product.name}) }}" class="mt-auto">Bekijken</a>
                        </div>
                    </div>
                </div>
            {% endfor %}

        </div>
    </div>

{% endblock %}
